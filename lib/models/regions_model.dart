import 'package:flutter/material.dart';

class RegionData {
  final String name;
  final IconData icon;
  final List<Color> gradientColors;

  const RegionData(this.name, this.icon, this.gradientColors);
}
